# 依赖（巨大，禁止上传）
scf/node_modules/
node_modules/
**/node_modules/

# npm 缓存（scf 残留垃圾，禁止上传）
scf/clean/

# 本地打包产物
frontend.zip
_zip.py

# 部署配置（含项目名，避免误提交真实密钥）
.env

# 系统 / 缓存
.DS_Store
*.pyc
__pycache__/

# 游戏安装包（单文件 >100MB，GitHub 拒绝 push；改由 EdgeOne 控制台手动上传到站点 /download/）
frontend/download/
